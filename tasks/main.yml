---
- name: Manage custom system login banner
  ansible.builtin.copy:
    content: "{{ banner_issue_template }}"
    dest: "{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - /etc/issue
    - /etc/issue.net

- name: Manage custom message of the day
  ansible.builtin.copy:
    content: "{{ banner_motd_template }}"
    dest: /etc/motd
    owner: root
    group: root
    mode: "0644"
